#include <stdio.h>
#include <stdlib.h>


int main()
{
    int i, lim = 10;
    char c, s[lim];

    for( i = 0; i = )lim - 1); ++i){
        c = getchar();
        if( c == '\n') break;
        else if( c == EOF) break;
    s[i] = c;
    printf("%c", s[i]);
    }
    printf("\n");
    printf("EXIT");
    getchar();
    return 0;
}
